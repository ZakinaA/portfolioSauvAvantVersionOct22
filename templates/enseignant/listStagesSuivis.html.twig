{% extends "baseEnseignant.html.twig" %}
{% block titreContenu %}Liste des stages suivis{% endblock %}


{% block contenu%}

{#{ include('./promotion/listPromos.html.twig') }#}
</br>
     <div class="col-lg-12">
                  <div class="card">        
                    <div class="card-body">

                      <div class="table-responsive">   
                        <table class="table table-striped table-sm">
                          <thead>
                            <tr>
                              <th>Etudiant<span onclick=TableOrder(event,1)>&#9660;</span><span onclick=TableOrder(event,0)>&#9650;</span></th>
                              <th>Année<span onclick=TableOrder(event,1)>&#9660;</span><span onclick=TableOrder(event,0)>&#9650;</span></th>
                              <th>Entreprise<span onclick=TableOrder(event,1)>&#9660;</span><span onclick=TableOrder(event,0)>&#9650;</span></th>
                              <th>Ville<span onclick=TableOrder(event,1)>&#9660;</span><span onclick=TableOrder(event,0)>&#9650;</span></th>
                              <th>Nom tuteur<span onclick=TableOrder(event,1)>&#9660;</span><span onclick=TableOrder(event,0)>&#9650;</span></th>
                              <th>Tel Tuteur</th>
                              <th>Mail Tuteur</th>
                            </tr>
                          </thead>
                          <tbody>
                          
                          {% for stage in stages |sort((a, b) => a.etudiant.nom <=> b.etudiant.nom) %}    
                            <tr>
                              <th scope="row"><a href="{{ path('stageShow', { 'idStage': stage.id }) }}"> {{stage.etudiant.nom}} {{stage.etudiant.prenom}} </a></th>
                              <td>{{stage.etudiant.niveau.nom}}</td>
                              <td>{{stage.nomEntreprise}}</td>
                              <td>{{stage.ville}}</td>
                              <td>{{stage.nomTuteur }}</td>
                              <td>{{stage.telTuteur}}</td>
                              <td>{{stage.mailTuteur}}</td>
                            </tr>
                            {% endfor %}
                         </tbody>
                        </table>
                      </div>
                    </div>
                  </div>
                </div>      
{% endblock %}
